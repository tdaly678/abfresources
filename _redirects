# Routing for the tab URLs (added 2026-08-05). NOTE: this site is a Cloudflare
# WORKER with static assets, not Cloudflare Pages — Workers Assets supports
# _redirects and _headers the same way. A git push does NOT deploy; deploying is
# a separate manual `wrangler deploy` step.
# 200 = rewrite: the URL stays in the address bar and index.html is served;
# the in-page router (see "URL Routing" section of index.html) opens the
# matching tab. Anything NOT listed here still falls through to 404.html,
# which handles the numeric feedback short codes (/12345 -> /?f=12345).

# Trailing-slash variants -> canonical (no trailing slash)
/classes/     /classes     301
/abfs/        /abfs        301
/key-dates/   /key-dates   301
/lefcu/       /lefcu       301
/videos/      /lefcu       301
/lefcu/joshua/      /lefcu/joshua       301
/lefcu/corebeliefs/ /lefcu/corebeliefs  301
/lefcu/training/    /lefcu/training     301
/lefcu/articles/    /lefcu/articles     301
/lefcu/tutorials    /lefcu/training     301
/lefcu/tutorials/   /lefcu/training     301
/teachers/    /teachers    301
/courses/     /courses     301
/special-visits/ /special-visits 301
/scheduling/  /scheduling  301
/feedback/    /feedback    301

# Friendly aliases
# NOTE: do NOT add trailing-slash-stripping rules for real directories like
# /corebeliefs-questions/ — Cloudflare Pages already 307s /foo -> /foo/ for a
# directory index, so a /foo/ -> /foo rule here creates a redirect loop. The
# tab routes above are safe because they are rewrites, not real directories.
/cbquestions  /corebeliefs-questions/  301
/cbquestions/ /corebeliefs-questions/  301
/dates        /key-dates   301
/dates/       /key-dates   301
/leaders      /abfs        301
/leaders/     /abfs        301
/resources    /lefcu       301
/resources/   /lefcu       301
/videos       /lefcu       301

# Short, emailable aliases for a single LEFC|U course. These are the links to
# paste into an email; they 301 to the canonical /lefcu/<course> URL.
/joshua        /lefcu/joshua       301
/joshua/       /lefcu/joshua       301
/corebeliefs   /lefcu/corebeliefs  301
/corebeliefs/  /lefcu/corebeliefs  301
# Tutorials retired from Online Studies 2026-09-03 — the walkthroughs now
# live in LEFC|U -> Training Videos.
/tutorials     /lefcu/training     301
/tutorials/    /lefcu/training     301

/mobilization  /special-visits  301
/mobilization/ /special-visits  301

# Tab routes -> the single-page hub
/classes      /            200
/abfs         /            200
/key-dates    /            200
/lefcu        /            200
/lefcu/joshua      /       200
/lefcu/corebeliefs /       200
/lefcu/training    /       200
/lefcu/articles    /       200
/teachers     /            200
/courses      /            200
/special-visits /          200
/scheduling   /            200
/feedback     /            200
